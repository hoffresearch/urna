{"id": "01-what-is-urna#1", "title": "what is a .urna file", "text": "a `.urna` file is a single, self-contained semantic knowledge base. one file carries the text chunks, their embeddings, the byte spans back to the original source, optional approximate-nearest-neighbor and lexical indices, and a search contract that says how the file is meant to be queried.", "source_uri": "demo/01-what-is-urna.md"}
{"id": "01-what-is-urna#2", "title": "what is a .urna file", "text": "you copy a `.urna` the way you copy a sqlite database: there are no companion files, no schema migration, and no external service to look up. the binary format is open and frozen at version one, so a file written today still opens years from now.", "source_uri": "demo/01-what-is-urna.md"}
{"id": "01-what-is-urna#3", "title": "what is a .urna file", "text": "the runtime memory-maps the file and scores embeddings directly off disk with simd dot products. there is no server and no central index. the file is the database.", "source_uri": "demo/01-what-is-urna.md"}
{"id": "02-forge-ingestion#1", "title": "what forge does", "text": "forge is the ingestion layer. it turns messy, heterogeneous inputs such as pdfs, plain text, datasets, and archives into a deterministic canonical intermediate that the urna build path already understands.", "source_uri": "demo/02-forge-ingestion.md"}
{"id": "02-forge-ingestion#2", "title": "what forge does", "text": "forge lives outside the sovereign core so its heavier dependencies never touch the small, frozen format and runtime crates. the rule that keeps builds reproducible is simple: the determinism anchor is the canonical extracted text plus the embedding model fingerprint, not the raw input. the same canonical text and the same fingerprint always produce a byte-identical file, even if the original pdf was re-saved or the extraction engine changed.", "source_uri": "demo/02-forge-ingestion.md"}
{"id": "02-forge-ingestion#3", "title": "what forge does", "text": "forge does not invent a second chunker. there is one authoritative chunker, and forge calls it, so chunk identities never drift between tools.", "source_uri": "demo/02-forge-ingestion.md"}
{"id": "03-citations#1", "title": "how citations work", "text": "every search hit comes back with a stable citation of the form `urna://content_hash/chunk_id`. the content hash is computed over the decoded canonical sections of the file, so it identifies the content rather than a particular copy or a particular compression choice.", "source_uri": "demo/03-citations.md"}
{"id": "03-citations#2", "title": "how citations work", "text": "because the citation points at content, two people who build the same logical corpus on two machines get the same citation, and a stored corpus and a compressed one cite identically. resolving a citation returns the exact canonical text and the original byte span it came from, which is what lets an agent quote a source it can prove.", "source_uri": "demo/03-citations.md"}
{"id": "03-citations#3", "title": "how citations work", "text": "the returned similarity score is a real cosine value, recomputed by an exact rerank, never an approximate proxy. a result you can cite is a result you can trust.", "source_uri": "demo/03-citations.md"}
{"id": "04-offline-sovereignty#1", "title": "offline by construction", "text": "urna is sovereign: the runtime never opens a socket, and every query is answered from the memory-mapped file on the local machine. nobody outside the operator has to be online, trusted, or even reachable for the database to work. that is the whole privacy story, and it needs no policy engine.", "source_uri": "demo/04-offline-sovereignty.md"}
{"id": "04-offline-sovereignty#2", "title": "offline by construction", "text": "to keep that promise for a brand-new user, the default embedder is a static, offline embedder that ships with the tool. it needs no model download and no network round-trip on first use, and it is deterministic, so a build is byte-identical and reproducible. a power user can bring a stronger embedding model instead, and the model's fingerprint is recorded so the corpus and the query embedder must agree or the search fails loudly.", "source_uri": "demo/04-offline-sovereignty.md"}
{"id": "04-offline-sovereignty#3", "title": "offline by construction", "text": "sovereign data is the priority: a corpus you build stays on the box you built it on, by construction rather than by promise.", "source_uri": "demo/04-offline-sovereignty.md"}
